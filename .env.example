# Puerto donde escucha el servidor (Render/Railway lo definen automÃ¡ticamente)
PORT=3000

# Ruta del archivo de base de datos SQLite.
# En local puedes dejarlo asÃ­ (se crea solo). En Render/Railway, si usas un
# disco persistente, apunta a la ruta montada, ej: /data/tomalo.db
DB_PATH=./data/tomalo.db
